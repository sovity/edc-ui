name: auto-merge

on:
  pull_request:

jobs:
  otherautomerge:
    needs: [secret_scan, license_scan]
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: write

    steps:
      - uses: fastify/github-action-merge-dependabot@v3

