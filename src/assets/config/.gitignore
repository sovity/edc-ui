# This file will be generated
# - in local dev before running ng serve
# - in docker container before starting nginx
app-config.json

# This file will be generated by the pipeline in order to store the current version of this build
# Contains last commit SHA, author and commit message
.version

# Any backup files / old files
app.config*.json
